# Where the bot's status API lives, as seen from the dashboard container.
PI_API=http://pihouse.local:8090

# Must match STATUS_TOKEN in pi-service/.env. Generate one with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
STATUS_TOKEN=
