# Copy to .env next to docker-compose.yml. Paths are host paths on the PC.

# Obsidian vault the session notes are moved into.
CIPHER_VAULT=C:/Users/Matthew/Desktop/D&D/New Game/Cipher

# Where session recordings are moved to. Deliberately OUTSIDE the vault —
# Obsidian would index the audio and gain nothing from it.
SESSION_AUDIO=C:/Users/Matthew/Desktop/D&D/New Game/Session Audio
