# Discord Developer Portal -> your application
DISCORD_TOKEN=
DISCORD_CLIENT_ID=

# Optional: force all notes into one specific channel instead of posting
# back into whichever text channel the voice session happened near.
NOTES_CHANNEL_ID=

# Your PC's static/reserved LAN IP running Ollama. Set OLLAMA_HOST=0.0.0.0
# on the PC and confirm firewall allows it on your Private network profile.
OLLAMA_URL=http://192.168.1.50:11434
OLLAMA_MODEL=qwen2.5:14b

# whisper.cpp model — smaller for the Pi's limited CPU. Options (in order of
# increasing size/accuracy/CPU cost): tiny.en, base.en, small.en, medium.en
WHISPER_MODEL_NAME=base.en
WHISPER_THREADS=4

# How aggressively to retry summarization when the PC is off
SUMMARIZE_RETRY_BASE_MS=60000
SUMMARIZE_RETRY_MAX_MS=1800000
SUMMARIZE_MAX_ATTEMPTS=0

# Auto-delete raw audio for completed sessions after this many days (0 = keep forever)
AUDIO_RETENTION_DAYS=14
