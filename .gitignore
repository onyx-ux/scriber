# Dependencies
node_modules/

# Secrets / local config — never commit these
.env
pi-service/rclone.conf

# Runtime data written by the bot (SQLite db, audio, whisper models, Obsidian export)
pi-service/data/
pi-service/models/
pi-service/obsidian-export/
*.db
*.db-journal
*.wav

# OS / editor cruft
.DS_Store
Thumbs.db
